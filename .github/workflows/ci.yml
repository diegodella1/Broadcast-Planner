name: CI

on:
  pull_request:
    branches: [main, dev/**]
  push:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: i18n catalogs
        run: npm run i18n:check
      - name: Service-role auth guard
        run: npm run security:service-role
      - name: Audit trail guard
        run: npm run security:audit-trail
      - name: Lint
        run: npm run lint
      - name: Format check
        run: npm run format:check
      - name: Tests + coverage
        run: npm test -- --coverage --run
      - name: Build
        run: npm run build
      - name: Dependency audit
        run: npm audit --omit=dev --audit-level=high
